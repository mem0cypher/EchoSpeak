{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-main",
  "description": "Narrow capability for the packaged EchoSpeak main, Settings, and companion windows.",
  "windows": ["main", "settings", "companion"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "desktop-runtime"
  ]
}
